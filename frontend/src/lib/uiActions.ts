// Centralized placeholder action for buttons across the app
// Keeps behavior consistent and easy to replace later
export function triggerAction(label: string): void {
  // Minimal, non-intrusive feedback for now
  // Replace this implementation with real analytics/toast/service calls later
  // eslint-disable-next-line no-console
  console.log(`[UI Action] ${label}`);
}


